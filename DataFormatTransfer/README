xml_Parser.py

从xml文件中获取文书文件标题及文件路径名，共三种解析方式SAX，DOM，Element Tree

sax从磁盘读取数据时，只读取部分当前需要的内容，对于大文件友好

DOM将整个文件读入内存，并以分层(树)的形式存储，对于大批量小文件友好

ElementTree是轻量级的DOM

html2json.py

将HTMl格式的文书文件，再将多个文书进行序列化，转换成一个json文件，json是轻量级的数据交换格式。



html2json.py

html_to_text 在线格式转换工具：

https://www.textfixer.com/html/html-to-text.php
